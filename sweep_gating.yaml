program: train.py
method: random
metric:
  name: reward/accuracy
  goal: maximize

parameters:
  config:
    value: config.json

  k:
    values: [2, 3, 5, 10]

  tau:
    values: [0.3, 0.5, 0.7]

  beta:
    values: [1.0, 2.0]

# Random search: 12 runs sampled from all combinations
# Grid would be 4 × 3 × 2 = 24 runs
count: 12
